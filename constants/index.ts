export const enum BoardKeys {
  ID = '_id',
  NAME = 'name',
  TOPICS = 'topics',
}

export const enum TopicKeys {
  ID = '_id',
  NAME = 'name',
  SUBJECTS = 'subjects',
}

export const enum SubjectKeys {
  ID = '_id',
  NAME = 'name',
  VALUE = 'value',
}
